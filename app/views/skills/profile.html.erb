<% content_for :body_type do%>
  userprofile
<% end %>

<div class="profilejumbotron">
	<div id="profilepicture">
		<img src="<%= @current_user.pic%>" class="circular" />
	</div>
</div>

<div class="profilepagecontainer">
<div class="profileinfo">
	<h1><%= current_user.username.capitalize %></h1>
	<h3> Lives in <%= current_user.zipcode.to_region%> </h3>
	<h4> Contact: <%= current_user.email %></h4>
	<%= link_to "Add Skill", new_skill_path, :class => 'btn btn-default' %>
</div>

<div class="col-md-6 col-md-offset-4" id="profilebio">
	<h1>Bio:</h1> <%= @current_user.bio %>
		<h1><%= current_user.username.capitalize %>'s Skills:</h1>
	<ul>
	  <% @user_skill.each do |s| %>
	    <li><h4><b><%= s.tag.upcase %></b>: <%=s.skill.capitalize%></h4>
	   	</li> 
	  <% end %>
	</ul>
		<h1><%= current_user.username.capitalize %> wants to learn:</h1>
	<ul>
	  <% @user_skill.each do |s| %>
	    <li><h4><b><%= s.learntag.upcase %></b>: <%=s.tolearn.capitalize %></h4></li>
	  <% end %>
	</ul>
</div>
</div>

</div>